query RunInput {
  paymentCustomization {
    metafield(namespace: "hideCOD", key: "cartAmount") {
      value
    }
  }
  cart {
    cost {
      totalAmount {
        amount
      }
    }
  }
  paymentMethods {
    id
    name
  }
}
