query RunInput {
  cart {
    attribute (key: "shippingArea") {
      key
      value
      __typename
    }
    deliveryGroups {
      deliveryOptions {
        code
        title
        handle
      }
    }
  }
}
